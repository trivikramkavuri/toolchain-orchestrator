# toolchain-orchestrator
A CLI tool for developers that intelligently orchestrates a sequence of common dev steps (lint, test, format, build, deploy preview) with conditional rules (e.g., “only run E2E tests if the PR title contains [e2e]”).
